---

# Переменные, необходимые для установки пакетов, применимые ко всем дистрибутивам в равной степени:

nginx_file_mode: "u=rw,g=r,o=r"
nginx_file_owner: "0"
nginx_file_group: "0"
nginx_package_list:
  - nginx

# Переменные, необходимые для конфигурирования Nginx, применимые на дистрибутиве семейства SUSE:

# Переменные, необходимые для конфигурирования Nginx, применимые на дистрибутиве семейства Redhat:

# Переменные, необходимые для конфигурирования Nginx, применимые на дистрибутиве семейства Debian:
