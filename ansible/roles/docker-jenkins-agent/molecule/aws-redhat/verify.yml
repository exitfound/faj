---
# Данный файл используется для выполнения шагов на предмет проверки запускаемых тестов:
- name: Verify our tests in Molecule
  hosts: all
  tasks:
    - name: Output the final result
      assert:
        that: true
